{
  "name": "piedra-papel-tijera",
  "version": "1.0.0",
  "description": "es un juego de piedra , papel o  tijeras contra la maquina",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && npm run copy-assets",
    "copy-assets": "copyfiles -u 1 src/imagen/* dist/imagen/* src/pages/*.html src/pages/*.css src/*.html src/*.css src/components/* dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.1.1"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^4.9.5"
  }
}